import { Point } from "pixi.js";


/**
 * the class that handle transformation coordinates
 * @extends Point
 */
export class Vector2 extends Point {

  constructor(x: number,y: number){
    super(x,y);
  }
}
